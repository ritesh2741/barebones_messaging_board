<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h2 class="title"><%= link_to post.title, post %> </h2>
    <p cass="date"><%=post.created_at.strftime("%B, %d, %Y")%></p>
  </div>
<% end%>